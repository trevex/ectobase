# Copyright 2026 ectobase contributors
# SPDX-License-Identifier: Apache-2.0
#
# Postgres + kine (etcd-v3 shim) for the hub aggregated apiserver's storage.
# Mirrors hub/hack/kine-up.sh (rancher/kine over Postgres). kine exposes an
# etcd-v3 gRPC endpoint on :2379 that the apiserver talks to via --etcd-servers.
# Storage here is ephemeral (emptyDir) — this is a dev/smoke manifest, not HA.
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "ectobase-hub.labels" . | nindent 4 }}
spec:
  selector:
    app: hub-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: {{ .Values.namespace }}
  labels:
    app: hub-postgres
    {{- include "ectobase-hub.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hub-postgres
  template:
    metadata:
      labels:
        app: hub-postgres
    spec:
      containers:
        - name: postgres
          image: {{ .Values.images.postgres }}
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          env:
            - name: POSTGRES_PASSWORD
              value: kine
            - name: POSTGRES_DB
              value: kine
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "postgres"]
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        - name: data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: kine
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "ectobase-hub.labels" . | nindent 4 }}
spec:
  selector:
    app: hub-kine
  ports:
    - name: etcd
      port: 2379
      targetPort: 2379
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kine
  namespace: {{ .Values.namespace }}
  labels:
    app: hub-kine
    {{- include "ectobase-hub.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hub-kine
  template:
    metadata:
      labels:
        app: hub-kine
    spec:
      containers:
        - name: kine
          image: {{ .Values.images.kine }}
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          args:
            - "--endpoint=postgres://postgres:kine@postgres:5432/kine?sslmode=disable"
            - "--listen-address=0.0.0.0:2379"
          ports:
            - containerPort: 2379
          readinessProbe:
            tcpSocket:
              port: 2379
            initialDelaySeconds: 5
            periodSeconds: 5
