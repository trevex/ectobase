{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": ["namespace", "agentNamespace", "reflectorAdmin"],
  "additionalProperties": true,
  "properties": {
    "namespace": {
      "type": "string",
      "description": "Namespace for hub infrastructure (apiserver, controller, kine, broker identity).",
      "minLength": 1
    },
    "agentNamespace": {
      "type": "string",
      "description": "Namespace for the netplane compiler and reflector.",
      "minLength": 1
    },
    "reflectorAdmin": {
      "type": "string",
      "description": "Address passed to hub-controller via -reflector-admin.",
      "minLength": 1
    },
    "imagePullPolicy": {
      "type": "string",
      "enum": ["Always", "IfNotPresent", "Never"]
    },
    "images": {
      "type": "object",
      "additionalProperties": true,
      "properties": {
        "hubApiserver": {"type": "string"},
        "hubController": {"type": "string"},
        "netplane": {"type": "string"},
        "kine": {"type": "string"},
        "postgres": {"type": "string"}
      }
    }
  }
}
