---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: vpcs.net.ectobase.dev
spec:
  group: net.ectobase.dev
  names:
    kind: VPC
    listKind: VPCList
    plural: vpcs
    singular: vpc
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VPC is an isolation domain (overlay network) identified by a
          VNI on the shared underlay.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VPCSpec is the desired state of a VPC (an isolation domain
              / overlay network).
            properties:
              defaultPolicy:
                description: |-
                  DefaultPolicy overrides the global default firewall posture for this VPC.
                  One of Allow (k8s semantics) or Deny (VPC-wide default-deny).
                type: string
              vni:
                description: |-
                  VNI optionally pins the VXLAN network identifier. When nil or 0, the VNI is
                  allocated by the central cluster from the global VNI space.
                format: int32
                type: integer
            type: object
          status:
            description: VPCStatus is the observed state of a VPC.
            properties:
              state:
                description: State is the current lifecycle state (e.g. Pending, Ready).
                type: string
              vni:
                description: VNI is the effective, allocated VXLAN network identifier.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
