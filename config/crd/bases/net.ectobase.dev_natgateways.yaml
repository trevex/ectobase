---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: natgateways.net.ectobase.dev
spec:
  group: net.ectobase.dev
  names:
    kind: NATGateway
    listKind: NATGatewayList
    plural: natgateways
    singular: natgateway
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          NATGateway is a drain-safe egress SNAT for the sources in a VPC, using
          deterministic (public-IP, port-block) allocation.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              NATGatewaySpec is the desired state of a NATGateway: a drain-safe egress SNAT
              for the sources in a VPC, using deterministic (public-IP, port-block) allocation.
            properties:
              edgeUnderlay:
                description: |-
                  EdgeUnderlay is DEPRECATED and IGNORED. The edge fleet self-advertises via
                  EDGE_UNDERLAY: egress (0.0.0.0/0 and 64:ff9b::/96 for NAT64) is originated by
                  any agent started with --edge-loopback, nexthop'd at that edge's own anycast
                  underlay. Retained only to avoid a CRD breaking change; set nothing here.
                type: string
              portsPerSource:
                description: |-
                  PortsPerSource is the deterministic port-block size handed to each source
                  (RFC 7422 / GCP-static style). Default 1024.
                format: int32
                type: integer
              publicIPs:
                description: PublicIPs is the pool of public IPv4s SNAT sources are
                  mapped onto.
                items:
                  type: string
                type: array
              vpcRef:
                description: VPCRef selects the VPC whose interfaces egress through
                  this gateway.
                properties:
                  name:
                    description: Name is the name of the referenced object.
                    type: string
                required:
                - name
                type: object
            required:
            - vpcRef
            type: object
          status:
            description: NATGatewayStatus is the observed state.
            properties:
              allocations:
                description: Allocations is the deterministic source→block table (published
                  to all gateways).
                items:
                  description: NATAllocation records one source's deterministic mapping.
                  properties:
                    portMax:
                      format: int32
                      type: integer
                    portMin:
                      format: int32
                      type: integer
                    publicIP:
                      description: PublicIP + [PortMin,PortMax] is the deterministic
                        block.
                      type: string
                    source:
                      description: Source is the overlay IP (a NetworkInterface IP)
                        being SNATed.
                      type: string
                  required:
                  - portMax
                  - portMin
                  - publicIP
                  - source
                  type: object
                type: array
              state:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
