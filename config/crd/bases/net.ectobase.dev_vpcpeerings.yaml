---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: vpcpeerings.net.ectobase.dev
spec:
  group: net.ectobase.dev
  names:
    kind: VPCPeering
    listKind: VPCPeeringList
    plural: vpcpeerings
    singular: vpcpeering
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VPCPeering is a scaffold-only resource. Mutual-consent VPC peering
          (§3.3).
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VPCPeeringSpec is the desired state of a VPCPeering.
            properties:
              exposedPrefixes:
                description: ExposedPrefixes are the CIDR prefixes this side exposes
                  to the peer VPC. Optional; if empty no prefixes are advertised.
                items:
                  type: string
                type: array
              peerVpcRef:
                description: PeerVPCRef references the peer VPC (may be in another
                  namespace).
                properties:
                  name:
                    description: Name is the name of the referenced VPC.
                    type: string
                  namespace:
                    description: Namespace is the namespace of the referenced VPC.
                    type: string
                required:
                - name
                - namespace
                type: object
              vpcRef:
                description: VPCRef references the local VPC that initiates the peering.
                properties:
                  name:
                    description: Name is the name of the referenced object.
                    type: string
                required:
                - name
                type: object
            required:
            - peerVpcRef
            - vpcRef
            type: object
          status:
            description: VPCPeeringStatus is the observed state of a VPCPeering.
            properties:
              message:
                description: Message is a human-readable explanation of the current
                  state.
                type: string
              state:
                description: State is the current lifecycle state of the peering (e.g.
                  Pending, Active, Error).
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
