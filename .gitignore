/.direnv
/target
*.o
# generated by git-hooks.nix (symlink into the nix store; machine-specific)
/.pre-commit-config.yaml
# `nix build` result symlink
/result
# Python bytecode cache
__pycache__/
*.pyc

# Brainstorming visual-companion session artifacts
.superpowers/

# mdbook build output (regenerate with `make docs`)
/docs/book

# Go binaries built by go build ./cmd/... in test/e2e (artifact, not source)
test/e2e/tap-dhcp-probe.bin
test/e2e/tap-dhcp-probe
test/e2e/netprobe

# mkdocs build output
site/

# Retired design archive: specs/plans live in git history, not tracked going forward.
docs/superpowers/

# Repo-local feedback notes: kept on disk under docs/superpowers/, not tracked.
/memory/
