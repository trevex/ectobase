/.direnv
/target
*.o
# generated by git-hooks.nix (symlink into the nix store; machine-specific)
/.pre-commit-config.yaml
# `nix build` result symlink
/result
# Python bytecode cache
__pycache__/
*.pyc

# Brainstorming visual-companion session artifacts
.superpowers/

# clab-up.sh renders these from the kind configs (absolute prefix mount paths)
hack/clab/*.gen
hack/clab/clab-*/

# mdbook build output (regenerate with `make docs`)
/docs/book
flowplane/nfkit/tests/data/l2fwd_out.pcap

# multicluster-e2e.sh renders this from patch.reflector.yaml.tmpl (env-substituted at run time)
test/e2e/fixtures/multicluster/agent-overlay/patch.reflector.yaml
