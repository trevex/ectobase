[package]
name = "dpdk-sys"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
doctest = false   # raw FFI only

[build-dependencies]
bindgen = "0.70"
cc = "1"
ureq = "2"      # tarball download (blocking, rustls)
sha2 = "0.10"   # checksum verification
