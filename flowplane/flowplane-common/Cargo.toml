[package]
name = "flowplane-common"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
aya = { workspace = true, optional = true }

[features]
default = []
user = ["dep:aya"]   # gates std-only impls (e.g. aya Pod) for the userspace side
