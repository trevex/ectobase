package config

// Derived holds values computed from the config (per-cluster prefixes).
// Task 3 fills this in; keep it as an empty struct + no-op for now.
type Derived struct{}

func (c *Config) derive() {}
