---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: compiledworkloads.platform.ectobase.dev
spec:
  group: platform.ectobase.dev
  names:
    kind: CompiledWorkload
    listKind: CompiledWorkloadList
    plural: compiledworkloads
    singular: compiledworkload
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CompiledWorkload is a compiled workload bound to a cluster.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CompiledWorkloadSpec defines the desired state of a CompiledWorkload.
            properties:
              clusterName:
                description: |-
                  ClusterName is the name of the cluster the workload is bound to
                  (the pod->node binding).
                type: string
              payload:
                description: Payload is the compiled workload payload.
                type: string
            type: object
          status:
            description: CompiledWorkloadStatus defines the observed state of a CompiledWorkload.
            properties:
              phase:
                description: Phase is the current lifecycle phase of the CompiledWorkload.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
